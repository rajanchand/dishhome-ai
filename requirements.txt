# Root requirements.txt — used by Vercel for the Python serverless function.
# Keep this lean: only what the backend needs to boot in serverless mode.
# Heavy/optional deps (redis, asyncpg, twilio, ollama etc.) are in
# backend/requirements.txt for local/Docker development.
fastapi==0.115.4
uvicorn==0.32.0
pydantic>=2.10.0
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.27.2
python-multipart==0.0.12
email-validator==2.2.0
argon2-cffi==23.1.0
